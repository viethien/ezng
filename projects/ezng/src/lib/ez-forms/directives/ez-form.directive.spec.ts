import { EzFormDirective } from './ez-form.directive';

describe('EzFormDirective', () => {
  it('should create an instance', () => {
    const directive = new EzFormDirective();
    expect(directive).toBeTruthy();
  });
});
